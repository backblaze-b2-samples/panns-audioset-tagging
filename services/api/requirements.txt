# Human-edited inputs — lower-bound (`>=`) pins only; a regression test enforces
# that (tests/test_dependency_lock.py). The exact resolution lives in
# requirements.lock (used by setup and CI); regenerate it per
# docs/dev-workflows.md#python-dependency-updates after editing this file.
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.18
python-dotenv>=1.0.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
boto3>=1.35.0

# Local PANNs AudioSet tagging engine (services/api/app/repo/panns_engine.py).
# Bounded pins to avoid the unpinned-ML clean-install trap: torch<2.5 keeps a
# CPU wheel that resolves on Python 3.12, numpy<2.0 stays numba/librosa-compatible,
# and librosa<0.11 matches the soundfile/soxr decode stack. The model checkpoint
# (~300 MB) auto-downloads to ~/panns_data on the first tag request, not here.
panns-inference>=0.1.1
torch>=2.2,<2.5
torchlibrosa>=0.1.0
librosa>=0.10,<0.11
soundfile>=0.14.0
numpy>=1.24,<2.0
numba>=0.58

# Dev / quality
ruff>=0.8.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
